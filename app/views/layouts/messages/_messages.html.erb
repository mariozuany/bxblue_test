<% flash.each do |key, value| %>
  <% if key == 'alert' %>
    <div class="alert alert-danger">
      <%= value %>
    </div>
  <% else %>
    <div class="alert alert-primary">
      <%= value %>
    </div>
  <% end %>
<% end %>